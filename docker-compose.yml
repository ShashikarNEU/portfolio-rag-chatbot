services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist SQLite chat history across container restarts
      - ./data:/app/data
    restart: unless-stopped
